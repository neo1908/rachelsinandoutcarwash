{
  "name": "rachels-in-and-out-carwash",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "deploy": "npm run build && wrangler pages deploy .svelte-kit/cloudflare --project-name=rachelsinandoutcarwash"
  },
  "devDependencies": {
    "@sveltejs/adapter-cloudflare": "^7.2.5",
    "@sveltejs/kit": "^2.0.0",
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "svelte": "^4.2.0",
    "svelte-check": "^3.6.0",
    "typescript": "^5.0.0",
    "vite": "^7.3.1"
  },
  "type": "module",
  "dependencies": {
    "@playwright/test": "^1.57.0"
  }
}
